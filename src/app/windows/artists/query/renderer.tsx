import { createRoot } from "react-dom/client";

import ArtistQueryWindowWrapper from "./ArtistQueryWindowWrapper";

import "@/styles/index.css";

const root = createRoot(document.body);
root.render(<ArtistQueryWindowWrapper />);
