import { ARTISTS_LIST_WINDOW } from "@/constants/appWindows";
import createWindow from "@/utils/windows";

const createArtistsListWindow = createWindow(ARTISTS_LIST_WINDOW);

export default createArtistsListWindow;
