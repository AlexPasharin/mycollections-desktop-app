/** Wait this long after the last keystroke before calling the API. */
export const SEARCH_DEBOUNCE_MS = 400;

/** Max number of search hits returned and shown in the list. */
export const ARTIST_ENTRIES_SEARCH_LIMIT = 10;
