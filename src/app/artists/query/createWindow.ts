import { ARTIST_QUERY_WINDOW } from "@/constants/appWindows";
import createWindow from "@/utils/windows";

const createArtistQueryWindow = createWindow(ARTIST_QUERY_WINDOW);

export default createArtistQueryWindow;
